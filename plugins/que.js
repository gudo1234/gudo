import fetch from 'node-fetch'

let handler  = async (m, { conn, usedPrefix, command, participants, groupMetadata }) => {
const more = String.fromCharCode(8206)
const readMore = more.repeat(4001)
conn.sendMessage(m.chat, { text: `🧀zo`, contextInfo: { externalAdReply: {title: `${wm}`, body: `${await conn.getName(m.chat)}`, thumbnailUrl: img.getRandom(), thumbnail: img.getRandom(), showAdAttribution: true, sourceUrl: canal}}} , { quoted: fkontak })
}
handler.customPrefix = /^(Que|que|qe|ke|Qe|k|Ke|Kee|Quee)$/i
handler.command = new RegExp
export default handler
